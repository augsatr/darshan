app = "darshan-api"
primary_region = "bom"

[build]
  dockerfile = "api/Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[env]]
  key = "PORT"
  value = "8080"
